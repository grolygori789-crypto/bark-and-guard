# Stage + Install Fix

- Stage 1 now uses FIT scaling only: no crop and no stretch.
- Extra ultra-wide space uses clean navy side rails, not black bars or duplicated scenery.
- Android/iOS install banner restored.
- Native beforeinstallprompt is used when Chromium exposes it.
- Manual browser install instructions are always available as fallback.
- Service worker bumped to v7.
