# Unified Fix

This package fixes both problem groups together:

1. Installed PWA launch / install flow
2. Stage 1 runtime display and interaction

## Included fixes
- Explicit GitHub Pages root paths under `/bark-and-guard/`
- PWA install button never becomes a dead button
- Service worker cache updated to v4
- Gameplay scene now uses true fullscreen cover scaling
- No fake duplicated side wallpaper
- Smaller HUD / smaller guard circles
- Phaser Arcade Physics enabled in `src/main.js`
- Touch interactions remain enabled on mobile
