# Upload batch

Extract this ZIP at the repository root and upload/replace files using the included canonical paths.

This batch intentionally replaces the previous Phaser prototype runtime with a self-contained Canvas runtime so PWA launch and touch input do not depend on an external CDN.
